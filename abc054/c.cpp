// 未AC
#include<bits/stdc++.h>
using namespace std;

int main(void){
	cin.tie(0);
	ios::sync_with_stdio(false);

	int n, m, x, y;
	vector<int> a, b;
	cin >> n >> m;
	for (int i = 0; i < n; ++i){
		cin >> x >> y;
		a.push_back(x);
		b.push_back(y);
	}
	return 0;
}